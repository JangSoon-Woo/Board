<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>
    <div class="wrap">
        <div class="login-form center">
            <form action="/login" method="POST">
                <h1 class="login-form-title">you are Robot!!!</h1>
                <% if (message) { %>
                <span style="color: red;"><%= message %></span>
                <% } %>
                <% if (user) { %> 
                    <!-- 로그인이 되잇을때 -->
                    <h2><%= user.name %>님 반갑습니다</h2>
                    <a href="/logout">로그아웃</a>
                <% }else { %>
                <!-- 로그인이 안되있을떄  -->
                <div>
                    <input type="text" name="id" class="login-form-input" placeholder="아이디">
                </div>
                <div>
                    <input type="text" name="pw" class="login-form-input" placeholder="비밀번호">
                </div>
                <div style="margin-top: 10px;">
                    <input type="checkbox" name="" id="rembmer-user"> <label for="rembmer-user" style="user-select: none;">로그인 유지하기</label>
                </div>
                <button class="login-form-submit">로그인</button>
                <a href="/registry">회원가입</a>
                <% } %>
                <p><a href="/posts">게시판</a></p>
            </form>
        </div>
    </div>
</body>
</html>